version: "3.9"

services:
  influx:
    image: influxdb:latest
    container_name: dev-monitoring-influx
    restart: always
    ports:
      - "8086:8086"
    volumes:
      - ./influx/data:/var/lib/influxdb2
      - ./influx/config:/etc/influxdb2
    environment:
      - DOCKER_INFLUXDB_INIT_MODE=setup
      - DOCKER_INFLUXDB_INIT_USERNAME=amir
      - DOCKER_INFLUXDB_INIT_PASSWORD=amir200300
      - DOCKER_INFLUXDB_INIT_ORG=BlackIQ
      - DOCKER_INFLUXDB_INIT_BUCKET=monitoring
      - DOCKER_INFLUXDB_INIT_ADMIN_TOKEN="N6oPPtgisTBUpUmtHd5YcjBh-ac13kxvTbh3T__P_iG89Z3eu3_-QXLVgi9rtu_zlbLW-sXWbQtuiiKAdf2Dog=="
